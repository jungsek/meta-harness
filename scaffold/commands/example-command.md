---
description: Example slash command
# targets: ["*"]
---

Run the test suite and summarize failures.
Delete this file after reading. Emitted to `.claude/commands/` (symlink),
`.cursor/commands/`, `.opencode/commands/`, `.agents/commands/`.
Codex custom prompts are global-only — skipped at project scope.
