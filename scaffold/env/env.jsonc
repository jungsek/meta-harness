{
  // Shared env vars → Claude settings `env` block + Codex [shell_environment_policy].
  "vars": {
    // "NODE_ENV": "development"
  }
  // Per-target extras:
  // "claude": { "env": { "CLAUDE_ONLY": "1" } },
  // "codex": { "shell_environment_policy": { "inherit": "all" } }
}
